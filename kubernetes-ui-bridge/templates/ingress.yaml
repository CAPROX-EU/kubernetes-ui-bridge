apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ui-bridge
  namespace: {{ .Release.Namespace | quote }}
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: ui-bridge
            port:
              name: http